# Copy to .env and fill in. Both are optional — CLI flags (--database-url,
# --bootnodes) override these, and hardcoded defaults apply if neither is set.
RETRACER_DATABASE_URL=postgres://retracer:retracer@localhost:5433/retracer
RETRACER_BOOTNODES=/ip4/127.0.0.1/tcp/30334/p2p/<peer-id>
